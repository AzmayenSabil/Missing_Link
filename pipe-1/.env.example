# ─────────────────────────────────────────────────────────────────────────────
# Phase 1 – environment configuration
# Copy this file to .env and fill in your values.
#   cp .env.example .env
# ─────────────────────────────────────────────────────────────────────────────

# OpenAI API key used by the generateSystemPrompts stage.
# When set, Phase 1 makes a single GPT-4o-mini call after all static-analysis
# stages complete and writes project-dna/system-prompts.json.
# If left blank, the LLM stage is silently skipped — all other artifacts are
# still produced.
OPENAI_API_KEY=
