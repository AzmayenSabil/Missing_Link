# ─────────────────────────────────────────────────────────────────────────────
# Copy these values into Railway → your service → Variables
# ─────────────────────────────────────────────────────────────────────────────

# Required: your OpenAI API key (used by all three pipes)
OPENAI_API_KEY=sk-...

# Optional: override the OpenAI model (defaults to gpt-4o)
# OPENAI_MODEL=gpt-4o

# Ports used internally — do NOT change unless you update vite.configs too
PORT_PIPE1=3001
PORT_PIPE2=3002
PORT_PIPE3=3003

# Railway exposes one public port via $PORT; the gateway reads this automatically
# PORT is set by Railway — do not set it yourself
