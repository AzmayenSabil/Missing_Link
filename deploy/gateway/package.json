{
  "name": "pipeline-gateway",
  "version": "1.0.0",
  "description": "HTTP gateway that routes traffic to the three pipe sub-servers",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "http-proxy-middleware": "^3.0.3"
  }
}
